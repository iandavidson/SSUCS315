Ian Davidson
Project4, Phase1
cs315 Spring2017
Dr. Kooshesh

    For phase one, all of the functions work as instructed. buildList() has a helper function which parses and
    assigns the vector of each recusive list respectively. In addition, I included a .cpp file for ListNode class,
    which is the implementation for printNode() and its helper function.y
